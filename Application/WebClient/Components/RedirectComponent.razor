@inherits ComponentBase
@inject NavigationManager Navigation

@code {
  [Parameter] public string RedirectUrl { get; set; } = RouteUtils.Home;
  protected override void OnAfterRender(bool firstRender) => Navigation.NavigateTo(RedirectUrl);
}